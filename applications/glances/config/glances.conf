[outputs]
# Glances 4.5+ validates Host headers in web/API mode.
# Caddy keeps the public Host header, while Homepage calls the Docker service name.
allowed_hosts=glances.home.arpa,glances,glances:61208,localhost,127.0.0.1
