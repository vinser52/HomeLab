services:
  openspeedtest:
    image: docker.io/openspeedtest/latest:v2.0.6
    container_name: openspeedtest
    restart: unless-stopped
    expose:
      # OpenSpeedTest serves HTTP on port 3000 inside the container.
      - "3000"
    networks:
      - proxy
